syntax = "proto2";
import "common/proto/config.proto";

message FieldConfig {
  // 字段名
  optional string field_name = 1;
  // 抽取方法
  optional string extract_method = 2;
}

message ExtractorConfig {
  // 消息名
  optional string message_name = 1;
  // 字段配置
  optional FieldConfig field_config = 2
}

